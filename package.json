{
  "name": "qrtuber-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "core",
    "extension",
    "docs",
    "webrtc"
  ],
  "scripts": {
    "compile": "npm run compile --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=24"
  }
}
